<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elden Compendium: Armor</title>
</head>
<body>
    <h1> AHMAH </h1>
    <nav>
        <a href="/elden">Home</a> <br> <br>
        <a href="/elden/new">Add an Entry!</a>
    </nav>
    <ul>
        <% for (let i = 0; i < arm.length; i++) { %>
            <li>
                The <a href='/arm/<%=arm[i].id %> '>
                    <%=arm[i].name; %>
                </a> <%=arm[i].found; %>
                <form action='/armor/<%=arm[i].id %>?_method=DELETE' method='POST'>
                    <input type='submit' value='Delete Entry'/>
                </form>
            <% } %>
        </ul>
</body>
</html>