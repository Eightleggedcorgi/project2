<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elden Compendium: Armor</title>
</head>
<body>
    <h1> AHMAH </h1>
    <nav>
        <h3><a href="/elden">Home</a>
        <a href="/armor/new">Add an Entry!</a> </h3>
    </nav>
    <ul>
        <% for (let i = 0; i < arm.length; i++) { %>
            <li>
                <a href='/armor/<%=arm[i].id %> '>
                    <h3><%=arm[i].name;%></a></h3>
                    <h4>Found: <%=arm[i].found; %> </h4>
                <form action='/armor/<%=arm[i].id %>?_method=DELETE' method='POST'>
                    <input type='submit' value='Delete Entry'/>
                </form>
                <a href="/armor/<%=arm[i].id %>/edit">Edit Entry</a> <br> <br>
            </li>
        <% } %>
    </ul>
</body>
</html>